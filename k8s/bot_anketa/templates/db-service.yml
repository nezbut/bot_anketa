apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.database.name }}-service"
spec:
  type: ClusterIP
  selector:
    components: {{ .Values.database.components }}
  ports:
  - port: {{ .Values.database.port }}